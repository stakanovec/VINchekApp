
//let countCR = countEntities(named: "PDF")
//Text("\(countCR)")
//func countEntities(named entityName: String) -> Int {
//    let request = NSFetchRequest<NSFetchRequestResult>(entityName: entityName)
//    do {
//        let count = try moc.count(for: request)
//        print("Количество объектов в сущности \(entityName): \(count)")
//        return count
//    } catch {
//        print("Ошибка при подсчете объектов в сущности \(entityName): \(error.localizedDescription)")
//        return 0
//    }
//}
